{{>header}}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <form>
                    <input type="text" class="from-control mt-5" placeholder="Location">
                    <input type="submit" class="ml-2">
                </form>
                <p id="searchValue" class="mt-5">Search loacation to get your weather!</p>
            </div>
        </div>
    </div>
{{>footer}}